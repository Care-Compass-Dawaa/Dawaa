# Optional: connect to your Java backend (AWS Lambda via API Gateway)
DAWAA_API_BASE_URL=""

# Optional: used only as a fallback for pharmacy search if no Java backend is set
LOVABLE_API_KEY=""
GOOGLE_MAPS_API_KEY=""
